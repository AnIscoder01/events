<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Événements pour le client</title>
    <!-- Intégration de Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
    <h1 class="text-center text-primary">Tous les événements pour le client l'id {{ clientId }}</h1>

    {% if listeE is empty %}
        <div class="alert alert-warning text-center mt-4" role="alert">
            Aucun événement trouvé pour ce client.
        </div>
    {% else %}
        <div class="table-responsive mt-4">
            <table class="table table-bordered table-striped table-hover">
                <thead class="table-dark">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Nom</th>
                    <th scope="col">Lieu</th>
                </tr>
                </thead>
                <tbody>
                {% for e in listeE %}
                    <tr>
                        <td>{{ e.id }}</td>
                        <td>{{ e.nom }}</td>
                        <td>{{ e.lieu }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}

    <div class="mt-4">
        <a href="{{ path('app_event') }}" class="btn btn-secondary">Retour à la liste des événements</a>
    </div>
</div>

<!-- Intégration de Bootstrap JS (optionnel pour des interactions) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>